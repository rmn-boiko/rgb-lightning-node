/**
 * @utexo/minimal-e2e — end-to-end suite against the repo's regtest stack.
 *
 * Not part of the default unit-test run; execute with `pnpm -C minimal-sdk e2e`
 * (see this package's README for the required local infrastructure). The
 * harness lives in setup.ts, the scripted user journey in flow.test.ts.
 */
export const E2E_NAME = '@utexo/minimal-e2e';
